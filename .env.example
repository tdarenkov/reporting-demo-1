# Copy to .env and set DATABASE_URL to your Postgres connection string.
# Any Postgres works — a local instance or any managed provider. The
# database should be named "warehouse".
DATABASE_URL=postgresql://user:password@host:5432/warehouse?sslmode=require
